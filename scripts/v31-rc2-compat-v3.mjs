import { spawnSync } from 'node:child_process';import path from 'node:path';import { mkdir,writeFile } from 'node:fs/promises';import { root,V3_VERSION } from './lib-v3-production.mjs';const assert=(c,m)=>{if(!c)throw new Error(m)};const versions=['26','25','24','23','22-1','22','21','20','19'];const rows=[];for(const v of versions){for(const kind of ['','-browser']){const name=`test:v31-alpha${v}${kind}`;const r=spawnSync('npm',['run',name],{cwd:root,encoding:'utf8',timeout:240000,maxBuffer:24*1024*1024});if(r.status!==0)throw new Error(`${name} failed\n${r.stdout}\n${r.stderr}`);rows.push({name,status:'pass'});}}assert(rows.length===18,'compat count');const report={version:V3_VERSION,generatedAt:new Date().toISOString(),status:'passed',smoke:9,chromium:9,rows};await mkdir(path.join(root,'reports'),{recursive:true});await writeFile(path.join(root,'reports/v31-rc2-compat.json'),JSON.stringify(report,null,2)+'\n');console.log('V3.1 RC2 Compat PASS：Alpha26→Alpha19 共 18 项 Smoke/Chromium 全部通过。');
