import { V3_VERSION } from './lib-v3-production.mjs';
import { assetTagPattern, importPattern } from './lib-v3-browser-page.mjs';
import { spawn } from 'node:child_process';
import { access,mkdtemp,readFile,rm } from 'node:fs/promises';
import os from 'node:os';import path from 'node:path';
const root=process.cwd(),sleep=ms=>new Promise(r=>setTimeout(r,ms)),assert=(c,m)=>{if(!c)throw new Error(m)};
let chromium=null;for(const c of [process.env.CHROMIUM,'/usr/bin/chromium','/usr/bin/chromium-browser','/usr/bin/google-chrome','/usr/bin/google-chrome-stable'].filter(Boolean)){try{await access(c);chromium=c;break}catch{}}
if(!chromium){console.warn('V3.1-alpha26 浏览器回归跳过：未找到 Chromium。');process.exit(0)}
async function waitJson(url,timeout=15000){const st=Date.now();while(Date.now()-st<timeout){try{const r=await fetch(url);if(r.ok)return await r.json()}catch{}await sleep(80)}throw new Error(`timeout ${url}`)}
async function waitTab(url,timeout=15000){const st=Date.now();while(Date.now()-st<timeout){try{const rows=await waitJson(url,1000),tab=rows.find?.(x=>x.type==='page')||rows[0];if(tab?.webSocketDebuggerUrl)return tab}catch{}await sleep(80)}throw new Error(`timeout tab ${url}`)}
class CDP{constructor(url){this.url=url;this.id=0;this.pending=new Map();this.events=[]}async connect(){this.ws=new WebSocket(this.url);await new Promise((r,j)=>{this.ws.onopen=r;this.ws.onerror=j});this.ws.onmessage=e=>{const m=JSON.parse(e.data);if(!m.id){this.events.push(m);return}const p=this.pending.get(m.id);if(!p)return;this.pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}}send(method,params={}){const id=++this.id;this.ws.send(JSON.stringify({id,method,params}));return new Promise((resolve,reject)=>this.pending.set(id,{resolve,reject}))}close(){try{this.ws?.close()}catch{}}}
const port=4992,server=spawn(process.execPath,['scripts/studio-v3.mjs','--host','127.0.0.1','--port',String(port)],{cwd:root,stdio:'ignore'});let chrome,cdp,userDataDir;
try{
 const health=await waitJson(`http://127.0.0.1:${port}/api/health`);assert(health.version===V3_VERSION,`health ${JSON.stringify(health)}`);
 const [html,css,studioJs,presetJs,recommenderJs,pubJs,mobileJs,issue]=await Promise.all([readFile('src/studio/index.html','utf8'),readFile('src/studio/studio.css','utf8'),readFile('src/studio/studio.js','utf8'),readFile('src/studio/design-presets.js','utf8'),readFile('src/studio/layout-recommender.js','utf8'),readFile('src/studio/publication-center.js','utf8'),readFile('src/studio/mobile-studio.js','utf8'),readFile('issues/001/issue.json','utf8').then(JSON.parse)]);
 const presetInline=presetJs.replace('export const DESIGN_PRESETS =','const DESIGN_PRESETS =').replace(/\nexport const DESIGN_PRESET_IDS[^;]+;\s*$/s,'\n');
 const recommenderInline=recommenderJs.replaceAll('export const ','const ').replaceAll('export function ','function ')+`\nSMART_LAYOUT_RECOMMENDER={SMART_LAYOUT_ENGINE_INFO,analyzeSmartLayoutProfile,recommendSmartLayouts};`;
 const pubInline=pubJs.replaceAll('export const ','const ').replaceAll('export function ','function ')+`\nPUBLICATION_CENTER={PUBLICATION_CENTER_INFO,metricTone,deviceLabel,publicationReadinessLabel,publicationOutputLabel};`;
 const mobileInline=`(()=>{${mobileJs.replaceAll('export const ','const ').replaceAll('export function ','function ')}\nMOBILE_STUDIO={MOBILE_STUDIO_BREAKPOINT,isMobileStudioViewport,mobileTextTargets,mobileMediaTargets,primaryEditableField,mobilePageSnapshot,mobileWorkflowItems};})();`;
 const studioInline=studioJs.replace(importPattern('./design-presets.js'),presetInline).replace("try { SMART_LAYOUT_RECOMMENDER = await import('./layout-recommender.js'); } catch {}",recommenderInline).replace("try { PUBLICATION_CENTER = await import('./publication-center.js'); } catch {}",pubInline).replace("try { MOBILE_STUDIO = await import('./mobile-studio.js'); } catch {}",mobileInline);
 const safe=JSON.stringify(issue).replaceAll('<','\\u003c');
 const mock=`<script>window.__BROWSER_ERRORS__=[];window.__V3_APP_BASE_OVERRIDE__='/';window.__V3_FORCE_WORKSPACE__=true;window.addEventListener('error',e=>window.__BROWSER_ERRORS__.push(String(e.error?.stack||e.message)));history.replaceState=()=>{};window.confirm=()=>true;window.prompt=(m,d)=>d||'';const ISSUE=${safe};const PUB={version:'3.1.0-alpha.26',issue:'001',metrics:{content:100,pageHealth:96,media:100,accessibility:92,links:{status:'pass',invalid:0},narration:{expected:18,found:18,complete:true,label:'18 / 18'}},devices:{mobile:'pass',desktop:'pass'},audit:{findings:[]},canPublish:true,reason:''};window.fetch=async(input,opts={})=>{const u=new URL(String(input),'http://studio.test'),p=u.pathname,m=opts.method||'GET';const body=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json'}});if(p==='/api/issues'&&m==='GET')return body([{id:'001',label:'第一期',status:'published',engine:'v3',pageCount:18}]);if(p==='/api/issues/001'&&m==='GET')return body(structuredClone(ISSUE));if(p==='/api/issues/001/draft'&&m==='GET')return body({exists:false});if(p==='/api/issues/001/draft'&&m==='PUT')return body({savedAt:new Date().toISOString()});if(p==='/api/issues/001/snapshots')return body({snapshots:[]});if(p==='/api/issues/001/assets')return body({writable:true,items:[],summary:{total:0,used:0,unused:0,size:'0 B'}});if(p==='/api/design-library')return body({styles:[]});if(p==='/api/templates')return body({templates:[]});if(p==='/api/layout-library')return body({layouts:[]});if(p==='/api/issues/001/live-preview')return body({ok:true});if(p==='/api/issues/001/publication/status')return body(PUB);if(p==='/api/issues/001/review-workspace')return body({version:1,issueId:'001',items:[]});return body({error:'mock '+m+' '+p},404)};<\/script>`;
 const fixture=html.replace(assetTagPattern('link','studio.css'),`<style>${css}</style>`).replace(assetTagPattern('script','studio.js'),`${mock}<script type="module">${studioInline.replaceAll('</script>','<\\/script>')}<\/script>`);
 userDataDir=await mkdtemp(path.join(os.tmpdir(),'jinchang-v31a26-'));const debugPort=13280+Math.floor(Math.random()*80);chrome=spawn('xvfb-run',['-a',chromium,'--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--remote-allow-origins=*',`--remote-debugging-port=${debugPort}`,`--user-data-dir=${userDataDir}`,'--no-first-run','about:blank'],{stdio:'ignore',detached:true});const tab=await waitTab(`http://127.0.0.1:${debugPort}/json/list`);cdp=new CDP(tab.webSocketDebuggerUrl);await cdp.connect();await Promise.all([cdp.send('Page.enable'),cdp.send('Runtime.enable')]);const ev=async expression=>{const r=await cdp.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text);return r.result?.value};const tree=await cdp.send('Page.getFrameTree');await cdp.send('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:2,mobile:true});await cdp.send('Page.setDocumentContent',{frameId:tree.frameTree.frame.id,html:fixture});let ready=false;for(let i=0;i<250;i++){if(await ev('window.__V3_STUDIO_READY__===true').catch(()=>false)){ready=true;break}await sleep(40)}assert(ready,`Studio fixture not ready ${JSON.stringify(cdp.events.slice(-6))}`);await sleep(120);
 const shell=await ev(`(()=>{const api=window.__V3_STUDIO__;return {active:api.state.mobileStudioActive,canvasMode:api.state.canvasMode,body:document.body.classList.contains('mobile-studio-mode'),dock:getComputedStyle(document.getElementById('mobileStudioDock')).display,canvas:getComputedStyle(document.querySelector('.block-canvas')).display,preview:getComputedStyle(document.getElementById('previewCard')).display,header:getComputedStyle(document.getElementById('mobileStudioHeader')).display};})()`);assert(shell.active&&shell.body&&!shell.canvasMode&&shell.dock!=='none'&&shell.canvas==='none'&&shell.preview!=='none'&&shell.header!=='none',`mobile shell failed ${JSON.stringify(shell)}`);
 const text=await ev(`(async()=>{const api=window.__V3_STUDIO__,pages=api.state.issue.pages;const pi=pages.findIndex(p=>(p.blocks||[]).some(b=>['paragraph','quote'].includes(b.type)));api.goToPage(pi);await new Promise(r=>setTimeout(r,50));const f=document.getElementById('builtPreviewFrame');f.src='about:blank';await new Promise(r=>setTimeout(r,20));f.contentWindow.__MOBILE_MSGS__=[];f.contentWindow.addEventListener('message',e=>{if(e.data?.source==='v3-studio')f.contentWindow.__MOBILE_MSGS__.push(e.data)});document.querySelector('[data-mobile-tab="text"]').click();await new Promise(r=>setTimeout(r,30));const target=document.querySelector('#mobileBottomSheet [data-mobile-edit-reader]'),id=target?.dataset.mobileEditReader||'';target?.click();await new Promise(r=>setTimeout(r,220));const msgs=f.contentWindow.__MOBILE_MSGS__||[];return {pi,id,sheetClosed:!document.getElementById('mobileBottomSheet').classList.contains('is-open'),editMessage:msgs.some(x=>x.type==='mobile-edit-block'&&x.blockId===id),modeMessage:msgs.some(x=>x.type==='mobile-studio-mode'),label:document.getElementById('mobileStudioPageLabel').textContent};})()`);assert(text.id&&text.sheetClosed&&text.editMessage&&text.label===`P${text.pi+1}`,`mobile text bridge failed ${JSON.stringify(text)}`);
 const media=await ev(`(async()=>{const api=window.__V3_STUDIO__,pages=api.state.issue.pages,pi=pages.findIndex(p=>(p.blocks||[]).some(b=>['image','video'].includes(b.type)));api.goToPage(pi);await new Promise(r=>setTimeout(r,40));document.querySelector('[data-mobile-tab="media"]').click();await new Promise(r=>setTimeout(r,25));return {pi,cards:document.querySelectorAll('#mobileBottomSheet .mobile-target-card').length,replace:document.querySelectorAll('#mobileBottomSheet [data-mobile-replace-media]').length,title:document.getElementById('mobileSheetTitle').textContent};})()`);assert(media.cards>=1&&media.replace>=1&&media.title.includes('图片'),`media sheet failed ${JSON.stringify(media)}`);
 const layout=await ev(`(async()=>{document.getElementById('mobileSheetClose').click();document.querySelector('[data-mobile-tab="layout"]').click();await new Promise(r=>setTimeout(r,25));return {recommend:document.querySelectorAll('#mobileBottomSheet [data-mobile-apply-recommendation]').length,presets:document.querySelectorAll('#mobileBottomSheet [data-mobile-layout-preset]').length,columns:document.querySelectorAll('#mobileBottomSheet [data-mobile-columns]').length};})()`);assert(layout.presets===4&&layout.columns===3,`layout sheet failed ${JSON.stringify(layout)}`);
 const page=await ev(`(async()=>{document.getElementById('mobileSheetClose').click();document.querySelector('[data-mobile-tab="page"]').click();await new Promise(r=>setTimeout(r,25));return {rows:document.querySelectorAll('#mobileBottomSheet [data-mobile-go-page]').length,flows:[...document.querySelectorAll('#mobileBottomSheet [data-mobile-workflow]')].map(x=>x.dataset.mobileWorkflow)};})()`);assert(page.rows===18&&page.flows.join(',')==='audit,review,publish',`page workflow failed ${JSON.stringify(page)}`);
 for(const vp of [{w:320,h:740},{w:768,h:1024}]){
  await cdp.send('Emulation.setDeviceMetricsOverride',{width:vp.w,height:vp.h,deviceScaleFactor:1,mobile:true});await ev('window.dispatchEvent(new Event("resize"))');await sleep(70);
  const bounds=await ev(`(async()=>{const api=window.__V3_STUDIO__;api.openMobileSheet('page');await new Promise(r=>setTimeout(r,280));const sheet=document.getElementById('mobileBottomSheet').getBoundingClientRect(),dock=document.getElementById('mobileStudioDock').getBoundingClientRect(),head=document.getElementById('mobileStudioHeader').getBoundingClientRect();return {active:api.state.mobileStudioActive,w:innerWidth,h:innerHeight,sheet:{l:sheet.left,r:sheet.right,t:sheet.top,b:sheet.bottom},dock:{l:dock.left,r:dock.right,b:dock.bottom},head:{l:head.left,r:head.right,t:head.top},overflow:document.documentElement.scrollWidth>innerWidth+1};})()`);
  assert(bounds.active&&!bounds.overflow&&bounds.sheet.l>=-1&&bounds.sheet.r<=bounds.w+1&&bounds.sheet.t>=-1&&bounds.sheet.b<=bounds.h+1&&bounds.dock.l>=-1&&bounds.dock.r<=bounds.w+1&&bounds.dock.b<=bounds.h+1&&bounds.head.l>=-1&&bounds.head.r<=bounds.w+1,`mobile bounds ${vp.w}x${vp.h} failed ${JSON.stringify(bounds)}`);
  await ev('window.__V3_STUDIO__.closeMobileSheet()');
 }
 // 手机横屏：iPhone 横屏宽度是 844/926，超过 820 的宽度断点。曾经只按宽度判断，
 // 结果横屏退回桌面界面——实测 844×390 下 60 个可见按钮有 28 个落在视口外。
 // 现在"矮视口"同样按手机界面处理，这里把两个横屏尺寸固定成断言。
 for(const vp of [{w:844,h:390},{w:926,h:428}]){
  await cdp.send('Emulation.setDeviceMetricsOverride',{width:vp.w,height:vp.h,deviceScaleFactor:1,mobile:true});await ev('window.dispatchEvent(new Event("resize"))');await sleep(160);
  const land=await ev(`(()=>{const api=window.__V3_STUDIO__,de=document.documentElement;const vis=id=>{const el=document.getElementById(id);if(!el)return false;const b=el.getBoundingClientRect();return b.width>0&&b.height>0&&b.bottom>0&&b.top<innerHeight};const disp=sel=>{const el=document.querySelector(sel);return el?getComputedStyle(el).display:null};const dock=document.getElementById('mobileStudioDock'),head=document.getElementById('mobileStudioHeader');const gb=el=>{if(!el)return null;const b=el.getBoundingClientRect();return {t:Math.round(b.top),b:Math.round(b.bottom),l:Math.round(b.left),r:Math.round(b.right)}};return {mobile:api.state.mobileStudioActive,overflow:de.scrollWidth-de.clientWidth,vh:innerHeight,vw:innerWidth,save:vis('mobileStudioSave'),pages:vis('mobileStudioPagePicker'),health:vis('mobileStudioHealth'),back:vis('mobileStudioBack'),dock:gb(dock),head:gb(head),canvas:disp('.block-canvas'),preview:disp('#previewCard'),editor:disp('#visualEditor')}})()`);
  assert(land.mobile,`横向 ${vp.w}×${vp.h} 未进入手机界面（横屏手机应走手机壳层）`);
  assert(land.overflow<=1,`横向 ${vp.w}×${vp.h} 文档横向溢出 ${land.overflow}px`);
  assert(land.save&&land.pages&&land.health&&land.back,`横向 ${vp.w}×${vp.h} 手机壳层控件不可见 ${JSON.stringify(land)}`);
  // 关键回归特征：横屏曾经退回桌面界面（桌面的画布/预览面板铺开、按钮大量落在视口外）。
  // 与竖屏同一条契约（见上面的 mobile shell 断言）：桌面画布必须隐藏，Reader 预览保留。
  assert(land.canvas==='none'&&land.preview!=='none',`横向 ${vp.w}×${vp.h} 桌面画布未隐藏或预览缺失 canvas=${land.canvas} preview=${land.preview}`);
  assert(land.dock&&land.dock.t>=-1&&land.dock.b<=land.vh+1,`横向 ${vp.w}×${vp.h} 底部工具栏不在视口内 ${JSON.stringify(land.dock)}`);
  assert(land.head&&land.head.t>=-1&&land.head.b<=land.vh+1,`横向 ${vp.w}×${vp.h} 顶栏不在视口内 ${JSON.stringify(land.head)}`);
  await ev('window.__V3_STUDIO__.closeMobileSheet()');
  await sleep(60);
 }
 await cdp.send('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});await ev('window.dispatchEvent(new Event("resize"))');await sleep(120);
 // 手机端"真的能改字"：走完 打开文字面板 → 改输入框 → 保存 → 模型变化 的往返
 const edit=await ev(`(async()=>{const api=window.__V3_STUDIO__,sleep=ms=>new Promise(r=>setTimeout(r,ms));const pages=api.state.issue.pages;let hit=null;for(let pi=0;pi<pages.length&&!hit;pi++){const bs=pages[pi].blocks||[];for(let bi=0;bi<bs.length;bi++){const b=bs[bi];if(b&&!b.richText&&typeof b.text==='string'&&b.text.trim()){hit={pi,bi};break}}}if(!hit)return {error:'no-editable-block'};api.goToPage(hit.pi);await sleep(80);api.openMobileSheet('text');await sleep(300);const ta=document.querySelector('#mobileSheetBody [data-mobile-quick-text]');const btn=document.querySelector('#mobileSheetBody [data-mobile-save-text]');if(!ta||!btn)return {error:'no-textarea-or-button',ta:Boolean(ta),btn:Boolean(btn)};const marker='手机端编辑回归标记';ta.value=marker;btn.click();await sleep(220);return {changed:api.state.issue.pages[hit.pi].blocks[hit.bi].text===marker,pi:hit.pi,bi:hit.bi}})()`);
 assert(edit&&edit.changed,`手机端文字编辑往返失败 ${JSON.stringify(edit)}`);
 await ev('window.__V3_STUDIO__.closeMobileSheet()');
 // 触控目标：手机模式下可见的可点控件不得小于 40px（实测过 34px 的「保存文字」按钮）
 await ev(`window.__V3_STUDIO__.openMobileSheet('text')`);await sleep(300);
 const small=await ev(`(()=>{const out=[];for(const el of document.querySelectorAll('button,a[href],input,textarea,select,[role="button"]')){const r=el.getBoundingClientRect(),cs=getComputedStyle(el);if(cs.display==='none'||cs.visibility==='hidden'||r.width===0||r.height===0)continue;if(r.bottom<0||r.top>innerHeight||r.right<0||r.left>innerWidth)continue;if(r.width<40||r.height<40)out.push({label:(el.id||el.textContent||el.tagName).trim().slice(0,18),w:Math.round(r.width),h:Math.round(r.height)});}return out})()`);
 assert(!small.length,`手机面板里有 ${small.length} 个小于 40px 的触控目标：${JSON.stringify(small.slice(0,5))}`);
 await ev('window.__V3_STUDIO__.closeMobileSheet()');
 // iOS Safari 会在聚焦 font-size < 16px 的输入框时自动放大页面（真机实测 14px 时放大到 1.142 倍，
 // 可视宽度 402→352，右侧内容被挤出屏幕，连「保存文字」按钮都会被挤出可视区）。
 // 手机面板里的输入控件必须 ≥16px，这条在 Chromium 里可通过 computed style 稳定断言。
 const inputFonts=await ev(`(()=>{const bad=[];for(const el of document.querySelectorAll('#mobileBottomSheet input,#mobileBottomSheet textarea,#mobileBottomSheet select')){const fs=parseFloat(getComputedStyle(el).fontSize)||0;if(fs<16)bad.push({tag:el.tagName.toLowerCase(),fontSize:fs});}return bad})()`);
 assert(!inputFonts.length,`手机面板里有 ${inputFonts.length} 个字号 <16px 的输入控件（iOS 会自动放大页面）：${JSON.stringify(inputFonts)}`);
 // 手机壳层容器必须只在壳层激活时显示。曾经这三条规则只按屏幕宽度生效，
 // 结果手机上打开管理页也会挂着顶栏与底部工具栏（死按钮），
 // 真机实测管理页 dock/header 都是可见的。
 const leak=await ev(`(()=>{const b=document.body,had=b.classList.contains('mobile-studio-mode');b.classList.remove('mobile-studio-mode');const g=id=>{const el=document.getElementById(id);return el?getComputedStyle(el).display:null};const out={dock:g('mobileStudioDock'),header:g('mobileStudioHeader'),sheet:g('mobileBottomSheet')};if(had)b.classList.add('mobile-studio-mode');return out})()`);
 assert(leak.dock==='none'&&leak.header==='none'&&leak.sheet==='none',`未激活手机壳层时容器仍然显示（管理页会挂一条死工具栏）：${JSON.stringify(leak)}`);
 for(const vp of [{w:1024,h:768},{w:1366,h:768},{w:1920,h:1080}]){
  await cdp.send('Emulation.setDeviceMetricsOverride',{width:vp.w,height:vp.h,deviceScaleFactor:1,mobile:false});await ev('window.dispatchEvent(new Event("resize"))');await sleep(80);const desktop=await ev(`({active:window.__V3_STUDIO__.state.mobileStudioActive,body:document.body.classList.contains('mobile-studio-mode'),dock:getComputedStyle(document.getElementById('mobileStudioDock')).display,overflow:document.documentElement.scrollWidth>innerWidth+1})`);assert(!desktop.active&&!desktop.body&&desktop.dock==='none'&&!desktop.overflow,`desktop restore ${vp.w}x${vp.h} failed ${JSON.stringify(desktop)}`);
 }

 // With a sheet open the shell must stay reachable. The sheet used to sit on
 // top of the 116px dock and the backdrop covered the header, so tapping
 // 返回 / 保存 / ‹ / › / P1编辑 did nothing while editing. The health pill was
 // also display:none, leaving page health unreachable on a phone.
 await cdp.send('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});
 await ev('window.dispatchEvent(new Event("resize"))');await sleep(220);
 const reach=await ev(`(async()=>{const api=window.__V3_STUDIO__;api.openMobileSheet('page');await new Promise(r=>setTimeout(r,260));
   const reachable=el=>{const r=el.getBoundingClientRect(),x=Math.round(r.x+r.width/2),y=Math.round(r.y+r.height/2);if(x<0||y<0||x>=innerWidth||y>=innerHeight)return false;const t=document.elementFromPoint(x,y);return Boolean(t)&&(t===el||el.contains(t)||t.contains(el))};
   const ids=['mobileStudioBack','mobileStudioPagePicker','mobileStudioHealth','mobileStudioSave','mobilePrevPage','mobileNextPage','mobileEditSummary'];
   const blocked=ids.filter(id=>{const el=document.getElementById(id);return el&&!reachable(el)});
   const health=document.getElementById('mobileStudioHealth');
   return {blocked,healthVisible:Boolean(health&&health.getBoundingClientRect().width),healthW:Math.round(health?health.getBoundingClientRect().width:0),healthH:Math.round(health?health.getBoundingClientRect().height:0)}})()`);
 assert(!reach.blocked.length,`an open sheet blocks shell controls: ${JSON.stringify(reach.blocked)}`);
 assert(reach.healthVisible&&reach.healthW>=44&&reach.healthH>=44,`health pill not a usable target ${JSON.stringify(reach)}`);
 await ev('window.__V3_STUDIO__.closeMobileSheet()');
 const tabToggle=await ev(`(async()=>{const api=window.__V3_STUDIO__,sleep=ms=>new Promise(r=>setTimeout(r,ms));api.openMobileSheet('layout');await sleep(160);document.querySelector('[data-mobile-tab="layout"]').click();await sleep(160);return api.state.mobileSheetOpen})()`);
 assert(tabToggle===false,'tapping the active dock tab should close the sheet');

 const errors=await ev('window.__BROWSER_ERRORS__');assert(!errors.length,`browser errors ${JSON.stringify(errors)}`);
 console.log('V3.1-alpha26 浏览器回归通过：320/390/768 Reader-first 边界、四 Bottom Sheet、Reader 编辑桥接、媒体/布局/页面流程，以及 1024/1366/1920 桌面自动恢复均正常。');
}finally{cdp?.close();if(chrome?.pid)try{process.kill(-chrome.pid,'SIGTERM')}catch{};server?.kill('SIGTERM');await sleep(180);if(userDataDir)await rm(userDataDir,{recursive:true,force:true}).catch(()=>{})}
