import path from 'node:path';
import crypto from 'node:crypto';
import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { V3_VERSION, exists, root } from './lib-v3-production.mjs';

export const PUBLICATION_OUTPUT_ROOT = process.env.V3_PUBLICATION_OUTPUT_ROOT
  ? path.resolve(process.env.V3_PUBLICATION_OUTPUT_ROOT)
  : path.join(root, 'outputs-v3');

const clampPercent=value=>Math.max(0,Math.min(100,Math.round(Number(value)||0)));
function textOfRich(node){if(!node||typeof node!=='object')return '';let out=node.type==='text'?String(node.text||''):'';for(const child of node.content||[])out+=textOfRich(child);return out;}
function blockText(block){if(!block||typeof block!=='object')return '';if(block.richText)return textOfRich(block.richText);return [block.text,block.title,block.caption,block.badge,block.quote,block.author].filter(Boolean).join(' ');}
function walkBlocks(rows,fn){for(const block of rows||[]){if(!block||typeof block!=='object')continue;fn(block);if(block.type==='container')for(const col of block.columns||[])walkBlocks(col?.blocks,fn);}}
function hasPageContent(page){let chars=0,nodes=0;walkBlocks(page?.blocks||[],b=>{nodes++;chars+=blockText(b).trim().length;if(['image','video','cards','toc','articleLink'].includes(b.type))chars+=20;});chars+=(page?.body||[]).join('').trim().length;return nodes>0&&chars>0;}
function validHttpish(value){const raw=String(value||'').trim();if(!raw)return true;if(raw.startsWith('#')||raw.startsWith('./')||raw.startsWith('../')||raw.startsWith('/')||raw.startsWith('assets/'))return true;try{const u=new URL(raw);return ['http:','https:','mailto:','tel:'].includes(u.protocol);}catch{return false;}}
function collectLinks(issue){const links=[];const rich=(node,ctx)=>{if(!node||typeof node!=='object')return;for(const mark of node.marks||[])if(mark?.type==='link')links.push({href:mark.attrs?.href||'',...ctx});for(const c of node.content||[])rich(c,ctx);};for(const [pi,page] of (issue.pages||[]).entries()){walkBlocks(page.blocks||[],b=>{for(const key of ['href','url','link'])if(b[key])links.push({href:b[key],page:pi+1,blockId:b.id||null});if(b.richText)rich(b.richText,{page:pi+1,blockId:b.id||null});});}return links;}
function accessibilityMetric(issue){let images=0,withAlt=0,video=0,withCaption=0;for(const page of issue.pages||[])walkBlocks(page.blocks||[],b=>{if(b.type==='image'){images++;if(String(b.alt||'').trim())withAlt++;}if(b.type==='video'){video++;if(String(b.caption||'').trim()||String(b.poster||'').trim())withCaption++;}});const mediaTotal=images+video;const mediaScore=mediaTotal?((withAlt+withCaption)/mediaTotal)*65:65;const titled=(issue.pages||[]).filter(p=>String(p.title||p.navTitle||'').trim()).length;const titleScore=(issue.pages?.length?titled/issue.pages.length:1)*25;const metaScore=[issue.publication,issue.label,issue.subtitle].filter(x=>String(x||'').trim()).length/3*10;return clampPercent(mediaScore+titleScore+metaScore);}
function contentCompleteness(issue){const metaFields=[issue.publication,issue.label,issue.subtitle,issue.publisher];const meta=metaFields.filter(x=>String(x||'').trim()).length/metaFields.length;const pages=issue.pages||[];if(!pages.length)return 0;const titled=pages.filter(p=>String(p.title||p.navTitle||'').trim()).length/pages.length;const filled=pages.filter(hasPageContent).length/pages.length;return clampPercent(meta*25+titled*25+filled*50);}
function pageHealth(issue,audit){const pages=issue.pages||[];if(!pages.length)return 0;let healthy=0;for(const page of pages){const title=String(page.title||page.navTitle||'').trim();const blocks=Array.isArray(page.blocks)?page.blocks:[];const over=blocks.length>80;const empty=!hasPageContent(page);if(title&&!over&&!empty)healthy++;}let score=healthy/pages.length*100;const pageProblems=[...(audit?.blockers||[]),...(audit?.warnings||[])].filter(x=>x?.location?.kind==='page'||Number.isInteger(x?.location?.page)||Number.isInteger(x?.page));score-=Math.min(20,pageProblems.length*2);return clampPercent(score);}
function mediaCompleteness(audit){const m=audit?.media;if(!m||!m.referenced)return 100;return clampPercent((Number(m.found||0)/Math.max(1,Number(m.referenced||0)))*100);}
function narrationMetric(issue,audit){const enabled=Boolean(issue.features?.narration);const n=audit?.narration||{};if(!enabled)return {expected:0,found:0,complete:true,label:'未启用'};const expected=Number(n.expected??issue.pages?.length??0),found=Number(n.found||0);return {expected,found,complete:expected===0||found>=expected,label:`${found} / ${expected}`};}
export function buildPublicationStatus(issue,audit=null,evidence={}){
  const links=collectLinks(issue),badLinks=links.filter(x=>!validHttpish(x.href));const content=contentCompleteness(issue),pageHealthValue=pageHealth(issue,audit),media=mediaCompleteness(audit),accessibility=accessibilityMetric(issue),narration=narrationMetric(issue,audit);const devices=evidence.devices||{mobile:'pending',desktop:'pending',checkedAt:null};const blockers=Number(audit?.blockers?.length||0),warnings=Number(audit?.warnings?.length||0),pageCount=Array.isArray(issue?.pages)?issue.pages.length:0;const releaseableStatus=['draft','review','ready','published'].includes(String(issue.status||'draft'));/* 状态只决定是否允许进入发布流程；质量指标和设备验收是提示项，真正阻断只保留页面存在性、审计 blocker 和不安全链接。 */const statusReady=releaseableStatus;const hardReady=statusReady&&pageCount>0&&blockers===0&&badLinks.length===0;const formalReady=hardReady&&devices.mobile==='pass'&&devices.desktop==='pass';const advisories=[];if(content<95)advisories.push(`内容完整度 ${content}%：建议补齐元数据、页面标题或正文`);if(pageHealthValue<90)advisories.push(`页面健康 ${pageHealthValue}%：建议在 Reader 中检查密度与留白`);if(media<100)advisories.push(`媒体完整度 ${media}%：建议补齐缺失资源`);if(badLinks.length)advisories.push(`发现 ${badLinks.length} 个不安全或格式错误链接：必须先修复`);if(!narration.complete)advisories.push(`朗读音频 ${narration.label}：建议补齐 TTS 或更新基线`);if(accessibility<90)advisories.push(`无障碍评分 ${accessibility}%：建议补充图片 ALT 与视频说明`);if(devices.mobile!=='pass'||devices.desktop!=='pass')advisories.push('设备兼容性验收为提示项，请在发布前完成 Edge/Safari/iPhone 回归');if(warnings)advisories.push(`还有 ${warnings} 项提示检查，请在发布中心查看`);
  const reason=!releaseableStatus?`当前状态“${issue.status||'未设置'}”不允许正式发布。`:!pageCount?'期刊至少需要 1 个页面。':blockers?`仍有 ${blockers} 项硬性阻断问题。`:badLinks.length?`仍有 ${badLinks.length} 个不安全或格式错误链接，修复后才能发布。`:formalReady?'硬性门禁已通过，质量与设备提示也已完成。':'硬性门禁已通过；质量指标和设备验收均为提示项，可直接正式发布。';
  return {version:V3_VERSION,issue:issue.id,label:issue.label||issue.id,status:issue.status||'draft',generatedAt:new Date().toISOString(),metrics:{content,pageHealth:pageHealthValue,media,accessibility,links:{status:badLinks.length?'fail':'pass',total:links.length,invalid:badLinks.length},narration},devices,audit:{readiness:audit?.readiness||'unknown',score:Number(audit?.score||0),blockers,warnings,findings:[...(audit?.blockers||[]),...(audit?.warnings||[])].slice(0,12)},canPublish:hardReady,hardReady,formalReady,advisories,outputs:evidence.outputs||{},lastPreflight:evidence.lastPreflight||null,reason};
}
export function publicationEvidenceFile(id){return path.join(PUBLICATION_OUTPUT_ROOT,String(id),'publication-evidence.json');}
export async function readPublicationEvidence(id){const file=publicationEvidenceFile(id);if(!(await exists(file)))return {version:V3_VERSION,issue:id,outputs:{}};try{return JSON.parse(await readFile(file,'utf8'));}catch{return {version:V3_VERSION,issue:id,outputs:{}};}}
export async function writePublicationEvidence(id,patch){const current=await readPublicationEvidence(id);const next={...current,...patch,outputs:{...(current.outputs||{}),...(patch.outputs||{})},issue:id,updatedAt:new Date().toISOString()};const file=publicationEvidenceFile(id);await mkdir(path.dirname(file),{recursive:true});await writeFile(file,`${JSON.stringify(next,null,2)}\n`,'utf8');return next;}
export async function sha256(file){const data=await readFile(file);return crypto.createHash('sha256').update(data).digest('hex');}
