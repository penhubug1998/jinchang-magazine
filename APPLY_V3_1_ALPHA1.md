# V3.1-alpha1

覆盖到完整项目后：

```bash
npm run verify:core
npm run studio:v3 -- --port 4188
```

专项浏览器：

```bash
npm run test:v31-alpha1-browser
```

V3.0.0 的最终发布脚本继续绑定 `v3StableVersion=3.0.0`，不要删除原 final gate 证据。
