# V3.1 Alpha33：预览遮挡与存储预检查

- Reader 实时测量页面底部与固定工具栏的覆盖关系，并向制作中心回传遮挡像素和布尔状态。
- 版面健康、预览状态和发布中心会提示“底部可能被工具栏遮挡”，给出收起工具栏、增加底部留白或拆页建议。
- 发布中心新增服务器剩余空间指标；空间偏低时给出提示，低于硬性安全线时阻断发布。
- 发布前检查新增服务器存储空间步骤，避免媒体上传或发布包生成阶段才暴露磁盘不足。
- 本阶段不修改任何期刊正文、文章链接、页数、媒体资源或发布状态。
