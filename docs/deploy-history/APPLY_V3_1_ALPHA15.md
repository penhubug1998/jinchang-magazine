# V3.1-alpha15 · 所见即所得工作区 / 制作中心瘦身

Alpha15 把 Alpha14 已有的受约束画布进一步提升为主要编辑方式，并收紧制作中心的信息架构：制作中心负责选刊、选页、治理与发布，Workspace 负责日常内容编辑和视觉排版。

## 核心变化

- 制作中心桌面端采用固定视口，期刊列表、页面列表和主区域独立滚动，避免整页需要长距离滚到底。
- 原常驻 ISSUE INFO 移入“期刊信息”弹窗；当前页提供醒目的“进入沉浸工作区 →”主操作。
- Workspace 支持编辑辅助 30/70、双栏 50/50、画布优先 25/75、纯画布四种布局；画布优先是真实 25/75，不再被旧 30% 最小值限制。
- 移除主界面的“快速结构”预览标签，真实 Reader 成为唯一主预览；结构信息仍由高级/管理能力承载。
- “编辑新窗口”打开完整 Workspace，而不是只打开 Reader，并继承期号、页码、布局、预览尺寸、编辑模式、画布模式和当前选择。
- Reader 画布支持双击文字原地编辑；提交仍通过 Studio state 写回标准块字段，而不是直接把 DOM 当作数据源。
- 浮动文字工具条提供字号增减、粗体、左/中/右对齐；复杂参数继续进入高级设计。
- 继续兼容 Alpha14 多选、批量格式、拖动排序、Resize 与吸附，以及 Alpha13 动态板块导入。

## 数据边界

- 不新增 `issue.json` 必填字段。
- 原地文字编辑只写既有标准字段：`text`、`title`、`case`、`warning` 等。
- 字体与对齐仍写既有 `design` 数据。
- Workspace 布局、选中项等属于编辑会话状态，不作为 Reader 正式运行时依赖。
- V3.0.0 正式发布门禁逻辑保持锁定。

## 当前第一版限制

- 原地文字编辑优先覆盖 paragraph、quote、cardline、casePair、coverMeta、blessing、producer 等常用文本块。
- 复杂富文本、跨块文字流、任意绝对坐标自由排版不在本版数据模型中。
- 多窗口当前通过 URL 继承编辑状态；实时跨窗口协同可作为后续增强，不属于 Alpha15 门禁。
